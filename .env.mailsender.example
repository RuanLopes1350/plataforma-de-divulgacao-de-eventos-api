# ==============================================================================
# ARQUIVO .env.mailsender - CONFIGURAÇÃO DO MAIL SENDER
# ==============================================================================
# Crie um arquivo .env.mailsender na raiz do seu projeto com estas variáveis
# ==============================================================================

# Configurações de E-mail
SENDER_EMAIL=seu-email@gmail.com
SENDER_PASSWORD=sua-senha-de-aplicativo

# Chave Mestra (use uma chave forte)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MASTER_KEY=sua-chave-mestra-super-secreta-aqui

# MongoDB (se usar MongoDB local no Docker)
# Se usar MongoDB Atlas, coloque a URL completa aqui
DB_URL=mongodb+srv://usuario:senha@cluster.mongodb.net/mailsender